#ifndef HEADER
#define HEADER

/**
 * ABS - macro function
 * 
 * Description:
 * macro thats calculate the absolut value of the geving number
 * @x: the number to calculate
 * 
 * Return:
 * return absolut value of x
*/
#define ABS(x)((x < 0) ? (-x) : (x))
#endif
